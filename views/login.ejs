<!--
    ==============================================
    LOGIN PAGE - Ella Rises
    ==============================================
    Simple login page with email/password form.
    
    Layout: Split screen with form on left, decorative image on right.
    
    On form submit, POST to /login which:
    - Validates credentials
    - Creates session if successful
    - Redirects to dashboard
    - Shows error_message if credentials invalid
    
    This page doesn't have a navbar (full-screen auth design),
    but does have a floating language toggle.
-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Ella Rises</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=DM+Serif+Display&display=swap" rel="stylesheet">
</head>
<body>
    <!-- 
        Floating language toggle in top-right corner
        Auth pages don't have a navbar, so we show the toggle separately
    -->
    <div class="auth-lang-switcher">
        <% if (currentLang === 'en') { %>
            <a href="/lang/es" class="lang-toggle" title="Cambiar a Español" aria-label="Switch to Spanish">🇪🇸</a>
        <% } else { %>
            <a href="/lang/en" class="lang-toggle" title="Switch to English" aria-label="Switch to English">🇬🇧</a>
        <% } %>
    </div>

    <!-- 
        Auth Container - Split screen layout
        Left side: form, Right side: decorative image
    -->
    <div class="auth-container">
        <div class="auth-form-section">
            <div class="auth-form-wrapper">
                <h1 class="auth-title">Welcome Back</h1>
                <p class="auth-subtitle">Sign in to your account dashboard</p>

                <!-- 
                    Error message display
                    Shown when login fails (wrong email/password)
                    error_message is passed from the server
                -->
                <% if (error_message && error_message.length > 0) { %>
                    <div class="error-message">
                        <%= error_message %>
                    </div>
                <% } %>

                <!-- Login form - POSTs to /login route in index.js -->
                <form action="/login" method="POST" class="auth-form">
                    <div class="form-group">
                        <label for="email">Email Address</label>
                        <input 
                            type="email" 
                            id="email" 
                            name="email" 
                            placeholder="your.email@example.com"
                            required
                            autofocus="autofocus"
                        >
                    </div>

                    <div class="form-group">
                        <label for="password">Password</label>
                        <input 
                            type="password" 
                            id="password" 
                            name="password" 
                            placeholder="Enter your password"
                            required
                        >
                    </div>

                    <button type="submit" class="auth-submit-btn">Sign In</button>
                </form>

                <!-- Sign Up Link -->
                <p class="auth-footer-text">
                    Don't have an account? <a href="/register" class="auth-link">Sign up today!</a>
                </p>
                
                <!-- Back to Home Link -->
                <p class="auth-footer-text">
                    <a href="/" class="auth-link">Back to Home</a>
                </p>
            </div>
        </div>

        <div class="auth-image-section">
            <img src="/public/images/umfa_mural.jpg" alt="Ella Rises Community">
        </div>
    </div>
</body>
</html>

